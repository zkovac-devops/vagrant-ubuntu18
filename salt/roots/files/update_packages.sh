#!/bin/bash

sudo apt-get update -yq			# Fetches the list of available updates
sudo apt-get upgrade -yq		# Strictly upgrades the current packages
sudo apt-get dist-upgrade -yq	# Installs updates (new ones)
